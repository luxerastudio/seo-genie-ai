import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SEO-Genie AI - AI-Powered SEO Content Generation",
  description: "Generate rank-ready SEO content in seconds with AI. Multi-language support, real-time SEO scoring, and enterprise-grade features.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
